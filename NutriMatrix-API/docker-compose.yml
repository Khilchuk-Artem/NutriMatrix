version: '3.4'

services:
  authdb:
    image: authdb
    
  auth.api:
    image: ${DOCKER_REGISTRY-}authapi
    build:
      context: .
      dockerfile: Auth.API/Dockerfile
    depends_on:
      - authdb


volumes:
  sql_data: